{
  "entities": {
    "Order": {
      "title": "Order",
      "description": "An order for a service from a potential client.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "clientEmail": { "type": "string", "format": "email" },
        "clientPhone": { "type": "string" },
        "country": { "type": "string" },
        "city": { "type": "string" },
        "serviceType": { "type": "string" },
        "message": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "contacted", "completed", "cancelled"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "budget": { "type": "number" },
        "paymentMethod": { "type": "string" },
        "preferredLanguage": { "type": "string" }
      },
      "required": ["firstName", "lastName", "clientEmail", "serviceType", "message", "status", "createdAt", "country", "paymentMethod"]
    },
    "UserProfile": {
      "title": "User Profile",
      "description": "Private profile information for the admin (Usama).",
      "type": "object",
      "properties": {
        "isAdmin": { "type": "boolean" },
        "email": { "type": "string", "format": "email" }
      },
      "required": ["isAdmin", "email"]
    }
  },
  "firestore": {
    "/orders/{orderId}": {
      "schema": "Order",
      "description": "Collection of service orders."
    },
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profiles and permissions."
    }
  }
}
